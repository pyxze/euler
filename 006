int squares = 0;
for (int i = 1; i <= 100; i++)
{
	squares += i*i;
}
Console.WriteLine(squares);

int square = 0;
for (int i = 1; i <= 100; i++)
{
	square += i;
}
Console.WriteLine(square*square);

Console.WriteLine(square * square - squares);
